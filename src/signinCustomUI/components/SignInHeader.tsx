import { Heading, View } from "@aws-amplify/ui-react";

export const SignInHeader = () => (
  <View textAlign={"center"} padding={8} backgroundColor="#333">
    <Heading color="#fff" level={3}>
      Order Goods
    </Heading>
  </View>
);
